# Strings are sequences of characters enclosed in quotes:
# Single quotes: 'hello'
# Double quotes: "hello"
# Triple quotes: '''hello''' or """hello""" (used for multiline strings)

message="Hello Sandeep MUhal  is hare"


pragraph="""Python strings come with a rich set of methods:

Case Manipulation:

.lower(), .upper(), .title(), .capitalize(), .swapcase()
Finding and Replacing:

.find(), .index(), .replace(), .startswith(), .endswith()
Splitting and Joining:

.split(), .rsplit(), .join()
Whitespace Handling:

.strip(), .rstrip(), .lstrip()
Counting and Checking:

.count(), .isalnum(), .isalpha(), .isdigit(), .islower(), .isupper()"""
# Case Mainuplation 
print(message.lower())
print(message.upper())
print(message.title())

print(message.capitalize())
print(pragraph.title())
